# Escaneos genómicos
Con los escaneos genómicos podemos explorar parámetros a lo largo de un genoma a fin de encontrar regiones con valores extremos. Cuando buscamos posibles señales de selección, se recomienda no excluir regiones codificantes o reguladoras. Por otro lado, si se recomienda incluir solo regione de alta calidad, obviando regiones con bajos puntajes de mapeo, profundidad de secuenciación excesivamente baja/alta, y regiones de baja complejidad que puedan confundir los análisis.

## Estimación de 2d-SFS
El espectro de frecuencia de sitios de dos dimensiones (2d SFS) es una extensión del 1d SFS para dos poblaciones. Primero, se obtiene el SFS para cada población. Creamos un script para someter al HPC con `nano 1-1dsfs`.
```bash
!/bin/bash

# Nombre del proceso:
#SBATCH --job-name=1-1dsfs

# Proyecto:
#SBATCH --account=tipo-iii
#SBATCH --partition=standard # Nombre de la partición

# Recursos:
#SBATCH --time=24:00:00 # Límite de tiempo de corrida
#SBATCH --cpus-per-task=15 # Límite de CPUs para cada tarea en tareas de múltiples instancias
#SBATCH --mem=25G # Límite de memoria

# Archivos de salida
#SBATCH --output=/data/courses/course2026/USERNAME/logs/1dsfs_o%j # Nombre del archivo de salida stdout
#SBATCH --error=/data/courses/course2026/USERNAME/logs/1dsfs_e%j # Nombre del archivo de error stderr

# Ambiente de trabajo
module load angsd
module list

# Mensaje informativo
echo "Tarea ${SLURM_JOB_ID} name ${SLURM_JOB_NAME} iniciado en:"
date
echo "Nodo:"
hostname

# Comandos a ejecutar
angsd_prefix="NORTH"
ref_genome="/data/courses/course2026/course_files/mapeo_variantes/ppa_v1.asm.fasta"
bam_list="/data/courses/course2026/course_files/escaneos_genomicos/NORTH.list"
threads=10
nind=$(wc -l $bam_list | cut -d' ' -f1)
let minind=$nind/2
callable_sites="/data/courses/course2026/course_files/escaneos_genomicos/NORTH.callable_sites.bed"

echo "Crear archivo de sitios válidos en formato de angsd a partir de un archivo bed"
date
cut -f1-3 $callable_sites | awk '{print $1 "\t" $2+1 "\t" $3}' > $angsd_prefix.angsdfile
angsd sites index $angsd_prefix.angsdfile

seq 1 14 | awk '{print "Chr" $1}' > Chrs.txt

echo "Estimar las probabilidades de genotipo para el grupo $angsd_prefix"
date
angsd -GL 2 -trim 5 -doMajorMinor 1 -doGlf 2 -doSaf 1 -doCounts 1 -nThreads $threads \
 -minMapQ 30 -minQ 20 -setMinDepthInd 2 -minInd $minind -remove_bards 1 -uniqueOnly 1 \
 -baq 1 -C 50 -ref $ref_genome -anc $ref_genome -b $bam_list \
  -rf Chrs.txt -sites $angsd_prefix.angsdfile -out $angsd_prefix

echo "Obtener el SFS 1d para el grupo $angsd_prefix"
date
realSFS $angsd_prefix.saf.idx -P $threads -fold 1 -seed 1 > $angsd_prefix.sfs

echo "Obtener indices de diversidad genetica por segmentos y por ventanas para $angsd_prefix"
date
realSFS saf2theta $angsd_prefix.saf.idx -outname $angsd_prefix.saf2theta -sfs $angsd_prefix.sfs -P $threads -fold 1 -seed 1
thetaStat do_stat $angsd_prefix.saf2theta.thetas.idx -outnames $angsd_prefix.saf2theta.thetas.idx.chrs
thetaStat do_stat $angsd_prefix.saf2theta.thetas.idx -win 50000 -step 10000 -outnames $angsd_prefix.saf2theta.thetas.idx.w50k.s10k

echo "Job finalizado en:"
date
```
Sometemos el proceso para los _n_ grupos en evaluación. Asegurarse de cambiar el `USERNAME` por el nombre de usuario. Tambien, cambiar los archivos para cada grupo en especifico ("NORTH", "SOUTH")
```bash
sbatch 1-1dsfs.sh
```
De manera similar al análisis de cada población, el 2d SFS nos permite estimar parámetros de diferenciacion genética entre poblaciones, tanto a escala global como en ventanas. Para ello, construimos el 2d SFS para comparar los grupos en pares. Comenzar creando el script con `nano 2-2dsfs.sh`:
```bash
!/bin/bash

# Nombre del proceso:
#SBATCH --job-name=2-2dsfs

# Proyecto:
#SBATCH --account=tipo-iii
#SBATCH --partition=standard # Nombre de la partición

# Recursos:
#SBATCH --time=24:00:00 # Límite de tiempo de corrida
#SBATCH --cpus-per-task=15 # Límite de CPUs para cada tarea en tareas de múltiples instancias
#SBATCH --mem=25G # Límite de memoria

# Archivos de salida
#SBATCH --output=/data/courses/course2026/USERNAME/logs/2dsfs_o%j # Nombre del archivo de salida stdout
#SBATCH --error=/data/courses/course2026/USERNAME/logs/2dsfs_e%j # Nombre del archivo de error stderr

# Ambiente de trabajo
module load angsd
module list

# Mensaje informativo
echo "Tarea ${SLURM_JOB_ID} name ${SLURM_JOB_NAME} iniciado en:"
date
echo "Nodo:"
hostname

# Comandos a ejecutar
angsd_prefix="NORTH.SOUTH"
ref_genome="/data/courses/course2026/course_files/mapeo_variantes/ppa_v1.asm.fasta"
group1_saf="/data/courses/course2026/course_files/escaneos_genomicos/NORTH.saf.idx"
group2_saf="/data/courses/course2026/course_files/escaneos_genomicos/SOUTH.saf.idx"
threads=10

echo "Obtener el SFS 2d para los grupos $angsd_prefix"
date
realSFS $group1_saf $group2_saf -P $threads -seed 1 > $angsd_prefix.sfs

echo "Obtener indices de diferenciacion genetica por segmentos y por ventanas para $angsd_prefix"
date
realSFS fst index $group1_saf $group2_saf $angsd_prefix.sfs -fold 1 -fstout $angsd_prefix
realSFS fst stats $angsd_prefix.fst.idx > $angsd_prefix.stats
realSFS fst stats2 $angsd_prefix.fst.idx -win 50000 -step 10000 > $angsd_prefix.w50k.s10k.stats

echo "Job finalizado en:"
date
```
Sometemos el proceso al organizador. Recordar cambiar `USERNAME` por el nombre de usuario. También se puede modificar el tamaño y sobrelape de las ventanas en el análisis por ventanas.
```bash
sbatch 2-2dsfs.sh
```
El análisis por ventanas nos permite comparar la tendencia general (_background_) con casos anómalos locales (_outliers_).

Para visualizar los resultados, usar herramientas como `ggplot` en R o IGV.
```r
## Packages
library(tidyverse)
library(viridis)
library(ggrepel)
library(gridExtra)
library(cowplot)
library(vegan)
library(ggh4x)
library(ggpubr)

## Working directory
getwd()

## Read metadata
#metadata <- read.table("2024-01_PopulationGenomics/2025-04-29_Expanded_Sample_summary2.txt", header = TRUE, sep = "\t")

## Read data
Fst_Window2 <- read.table("Desktop/curso/All2_pairs_window.merged.stats", header = TRUE)
Window_Groups <-  read.table("Desktop/curso/All2_groups_window.merged.stats.filtered", col.names = c("Group","Chr","midPos","tW","tP","tF","tH","tL","tajD","fulif","fuliD","fayH","zengsE","NSites"))

## Compare nucleotide diversity and Taj D across chromosomes
# Create plotting functions for tP, tW and tajD
plot_tP_Chrs_win(Window_Groups, "North_3.2", "Chr2", "red")
plot_tP_Chrs_win <- function(dataset, pop, Chromosome, color){
  ggplot(subset(subset(subset(dataset, Group == pop), Chr == Chromosome),NSites > 10000), aes(x = midPos/1000, y = tP/NSites)) +
    geom_line(linewidth = 0.5, color = color) + theme_bw() + 
    theme(panel.grid.minor.x = element_blank(),
          panel.grid.major.x = element_blank(),
          axis.text = element_blank(),
          axis.ticks = element_blank(),
          axis.title.y = element_blank(),
          axis.title.x = element_text(size = 10),
          plot.margin = margin(t = 0, r = 0, b = 0, l = 0),
          panel.border = element_blank()) +
    scale_x_continuous(expand = c(0, 0)) +
    coord_cartesian(ylim = c(0, 0.0175)) +
    scale_y_continuous(expand = c(0,0)) +
    xlab(Chromosome)+
    geom_hline(yintercept = 0)
}
plot_tW_Chrs_win <- function(dataset, pop, Chromosome, color){
  ggplot(subset(subset(subset(dataset, Group == pop), Chr == Chromosome),NSites > 10000), aes(x = midPos/1000, y = tW/NSites)) +
    geom_line(linewidth = 0.5, color = color) + theme_bw() + 
    theme(panel.grid.minor.x = element_blank(),
          panel.grid.major.x = element_blank(),
          axis.text = element_blank(),
          axis.ticks = element_blank(),
          axis.title.y = element_blank(),
          axis.title.x = element_text(size = 10),
          plot.margin = margin(t = 0, r = 0, b = 0, l = 0),
          panel.border = element_blank()) +
    scale_x_continuous(expand = c(0, 0)) +
    coord_cartesian(ylim = c(0, 0.0175)) +
    scale_y_continuous(expand = c(0,0)) +
    xlab(Chromosome)+
    geom_hline(yintercept = 0)
}
plot_tajD_Chrs_win <- function(dataset, pop, Chromosome, color){
  ggplot(subset(subset(subset(dataset, Group == pop), Chr == Chromosome),NSites > 10000), aes(x = midPos/1000, y = tajD)) +
    geom_line(linewidth = 0.5, color = color) + theme_bw() + 
    theme(panel.grid.minor.x = element_blank(),
          panel.grid.major.x = element_blank(),
          axis.text = element_blank(),
          axis.ticks = element_blank(),
          axis.title.y = element_blank(),
          axis.title.x = element_text(size = 10),
          plot.margin = margin(t = 0, r = 0, b = 0, l = 0),
          panel.border = element_blank()) +
    scale_x_continuous(expand = c(0, 0)) +
    coord_cartesian(ylim = c(-3.25, 3.25)) +
    scale_y_continuous(expand = c(0,0)) +
    xlab(Chromosome)+
    geom_hline(yintercept = 0)
}
# Plot for each chromosome of each group
pops <- c("North_3.2","South_1.2")
colors <- matrix(c("firebrick4", "firebrick2", "darkorchid3", "orchid2", "aquamarine4", "aquamarine2"), ncol = 2, byrow = TRUE)
# Observed nucleotide diversity
tPlist <- list()
for(j in 1:2){
  for(i in 1:14){
    k <- (14*(j-1)+i)
    print(paste0("Chr", i, " Group ", pops[j], " index ", k))
    if(i == 1){
      tPlist[[k]] <- plot_tP_Chrs_win(Window_Groups, pops[j], paste0("Chr",i), colors[j,1]) + geom_vline(xintercept = 0) + theme(axis.text.y = element_text(size = 7))
    } else {
        if((i %% 2) == 1){
          tPlist[[k]] <- plot_tP_Chrs_win(Window_Groups, pops[j], paste0("Chr",i), colors[j,1])
          } else {
            tPlist[[k]] <- plot_tP_Chrs_win(Window_Groups, pops[j], paste0("Chr",i), colors[j,2])
          }
    }
  }
}  

tP_scan <- plot_grid(plotlist = tPlist, ncol = 14)

# Expected nucleotide diversity
for(j in 1:2){
  for(i in 1:14){
    k <- (14*(j-1)+i)
    print(paste0("Chr", i, " Group ", pops[j], " index ", k))
    if(i == 1){
      tPlist[[k]] <- plot_tP_Chrs_win(Window_Groups, pops[j], paste0("Chr",i), colors[j,1]) + geom_vline(xintercept = 0) + theme(axis.text.y = element_text(size = 7))
    } else {
        if((i %% 2) == 1){
          tPlist[[k]] <- plot_tP_Chrs_win(Window_Groups, pops[j], paste0("Chr",i), colors[j,1])
          } else {
            tPlist[[k]] <- plot_tP_Chrs_win(Window_Groups, pops[j], paste0("Chr",i), colors[j,2])
          }
    }
  }
}  

tWlist <- list()
for(j in 1:2){
  for(i in 1:14){
    k <- (14*(j-1)+i)
    print(paste0("Chr", i, " Group ", pops[j], " index ", k))
    if(i == 1){
      tWlist[[k]] <- plot_tW_Chrs_win(Window_Groups, pops[j], paste0("Chr",i), colors[j,1]) + geom_vline(xintercept = 0) + theme(axis.text.y = element_text(size = 7))
    } else {
      if((i %% 2) == 1){
        tWlist[[k]] <- plot_tW_Chrs_win(Window_Groups, pops[j], paste0("Chr",i), colors[j,1])
      } else {
        tWlist[[k]] <- plot_tW_Chrs_win(Window_Groups, pops[j], paste0("Chr",i), colors[j,2])
      }
    }
  }
}  
tW_scan <- plot_grid(plotlist = tWlist, ncol = 14)

# Tajima's D
tajDlist <- list()
for(j in 1:2){
  for(i in 1:14){
    k <- (14*(j-1)+i)
    print(paste0("Chr", i, " Group ", pops[j], " index ", k))
    if(i == 1){
      tajDlist[[k]] <- plot_tajD_Chrs_win(Window_Groups, pops[j], paste0("Chr",i), colors[j,1]) + geom_vline(xintercept = 0) + theme(axis.text.y = element_text(size = 7))
    } else {
      if((i %% 2) == 1){
        tajDlist[[k]] <- plot_tajD_Chrs_win(Window_Groups, pops[j], paste0("Chr",i), colors[j,1])
      } else {
        tajDlist[[k]] <- plot_tajD_Chrs_win(Window_Groups, pops[j], paste0("Chr",i), colors[j,2])
      }
    }
  }
}  

tajD_scan <- plot_grid(plotlist = tajDlist, ncol = 14)

## Compare Fst values for pairwise comparisons
# Create plotting function
plot_Fst_Chrs_win <- function(dataset, pop_comp, Chromosome, color){
  ggplot(filter(dataset, Pair == pop_comp, Chr == Chromosome, NSites > 10000), aes(x = midPos/1000, y = FST)) +
    geom_line(linewidth = 0.5, color = color) + theme_bw() + 
    theme(panel.grid.minor.x = element_blank(),
          panel.grid.major.x = element_blank(),
          axis.text = element_blank(),
          axis.ticks = element_blank(),
          axis.title.y = element_blank(),
          axis.title.x = element_text(size = 10),
          plot.margin = margin(t = 0, r = 0, b = 0, l = 0),
          panel.border = element_blank()) +
    scale_x_continuous(expand = c(0, 0)) +
    coord_cartesian(ylim = c(0, 0.65)) +
    scale_y_continuous(expand = c(0, 0)) +
    xlab(Chromosome) +
    geom_hline(yintercept = 0)
}


# Create and fill plot list
fst_colors <- matrix(c("firebrick3", "orchid2", "darkorchid3", "aquamarine2", "aquamarine4", "firebrick2"), ncol = 2, byrow = TRUE)
Fstlist <- list()
for(i in 1:14){
    k <- (i)
    print(paste0("Chr", i, " Groups ", "North_3.2-South_1.2", " index ", k))
    quantfst <- quantile(filter(Fst_Window2, Pair == "North_3.2-South_1.2", NSites > 10000)$FST, probs = 0.9975)
    if(i == 1){
      Fstlist[[k]] <- plot_Fst_Chrs_win(Fst_Window2, "North_3.2-South_1.2", paste0("Chr",i), fst_colors[j,1]) + geom_vline(xintercept = 0) + theme(axis.text.y = element_text(size = 7)) + geom_hline(yintercept = quantfst, linetype = "dashed", color = "black")
    } else {
      if((i %% 2) == 1){
        Fstlist[[k]] <- plot_Fst_Chrs_win(Fst_Window2, "North_3.2-South_1.2", paste0("Chr",i), fst_colors[j,1]) + geom_hline(yintercept = quantfst, linetype = "dashed", color = "black")
      } else {
        Fstlist[[k]] <- plot_Fst_Chrs_win(Fst_Window2, "North_3.2-South_1.2", paste0("Chr",i), fst_colors[j,2]) + geom_hline(yintercept = quantfst, linetype = "dashed", color = "black")
      }
    }
}
fst_scan <- plot_grid(plotlist = Fstlist, ncol = 14, labels = c("A", rep("",13), "B", rep("",13), "C", rep("",13)), label_x = c(rep(0.1,36)), label_y = c(rep(1,36)), label_size = 30)



############ 2
Fst_Window2$Pair_1 <- sapply(strsplit(x = Fst_Window2$Pair, "-"), "[[", 1)
Fst_Window2$Pair_2 <- sapply(strsplit(x = Fst_Window2$Pair, "-"), "[[", 2)
tempfst <- filter(Fst_Window2, Pair %in% c("North_2-South_1.2", "North_2-South_A.1", "North_3.1-South_1.2", "North_3.1-South_A.1", "South_1.1-South_1.2", "South_1.2-South_A.1", "South_1.1-South_A.1"), NSites > 2000)
tempfst$Chr <- factor(tempfst$Chr,levels = c(paste0("Chr", 1:14)))
tempfst <- tempfst %>% group_by(Pair_1) %>% mutate(quant = quantile(FST, 0.999)) %>% ungroup()
fst_win <- ggplot(tempfst, aes(x = midPos/1000000, y = FST)) +
  geom_point(size = 0.5, alpha = 0.6, aes(color = Pair_2)) + theme_bw() + 
  theme(panel.grid.minor.x = element_blank(),
        panel.grid.major.x = element_blank(),
        #        panel.spacing = unit(0.5, "lines"),
        axis.title.x = element_text(size = 12),
        #        plot.margin = margin(t = 0, r = 0, b = 0, l = 0),
        legend.position = "bottom",
        legend.title = element_text(hjust = 0.5, size = 15),
        legend.text = element_text(size = 12),
        strip.placement = "outside",
        axis.text = element_text(size = 12),
        axis.title = element_text(size = 15),
        strip.text = element_text(size = 12)) +
  scale_x_continuous(n.breaks = 3) +
  #  coord_cartesian(ylim = c(0, 1)) +
  scale_y_continuous(expand = c(0, 0), n.breaks = 4) +
  scale_color_manual(values = c("cadetblue3", "hotpink4"), name = "2nd population") +
  labs(x = "Position (Mbp)", y = "Pairwise FST value") +
  facet_grid( Pair_1 ~ Chr, drop = TRUE, scales = "free", space = "free_x") +
  #  facet_wrap( ~ Group + Chr, drop = TRUE, scales = "free_x", nrow = 5, strip.position = "bottom", labeller = function(x){x[2]}) +
  guides(color = guide_legend(override.aes = list(size = 4, alpha = 1))) +
  geom_hline(aes(yintercept = quant), linetype = "dashed")
```

## Análisis de enriquecimiento funcional de genes
Un análisis de enriquecimiento funcional de genes se refiere a la exploración de patrones biológicos sobrerrepresentados en genes seleccionados bajo algún criterio a lo largo del genoma. En este caso, el criterio es que se encuentren cerca a alguna señal de selección, por lo que el análisis nos permitirá explorar las tendencias de mecanismos moleculares y procesos biológicos que podrían estar bajo selección.

```r
## Packages
library(tidyverse)
library(topGO)

## Working directory
getwd()

## Read data
SouthvsNorth <- read.table("Desktop/curso/2025-03-22_GSouthNorth_outwinds_0.995_nameterms.txt", sep = "\t", col.names = c("Name", "GOterms"))
geneID2GO <- readMappings(file = "Desktop/curso/Allgene_nameterms.txt")
gff_Npal <- read.table("2024-01_PopulationGenomics/2024-6-02_Primary_transcripts_Npallida.gff", sep = "\t", quote = "")

## Enrichment analysis
# Prepare input global data
GO2geneID <- inverseList(geneID2GO)
geneNames <- names(geneID2GO)
# Enrichment analysis for all datasets
enrich_list <- list(NorthvsMid, MidvsSouth, SouthvsNorth, pcadapt_sites)
sig_terms <- data.frame(Dataset = "a", Category = "b", Term = "c", GO.ID = "d", classicFisher = 1)[-1,]
for(i in 1:4){
  # Prepare dataset data
  dat_name <- c("NorthvsMid", "MidvsSouth", "SouthvsNorth", "pcadapt")
  genes <- enrich_list[[i]][,1]
  geneList <- factor(as.integer(geneNames %in% genes))
  names(geneList) <- geneNames
  # BP terms
  BPGOdata <- new("topGOdata", ontology = "BP", allGenes = geneList, annot = annFUN.gene2GO, gene2GO = geneID2GO, nodeSize = 10)
  resultBPFisher <- runTest(BPGOdata, algorithm = "weight01", statistic = "fisher")
  BPterms <- GenTable(BPGOdata, classicFisher = resultBPFisher, orderBy = "classicFisher", ranksOf = "classicFisher", topNodes = 300)
  bp <- BPterms %>% filter(classicFisher < 0.01) %>% dplyr::select(any_of(c("GO.ID", "Term", "classicFisher")))
  if(nrow(bp) == 0){
    print(paste0(nrow(bp)," significant BP terms for dataset ", dat_name[i]))
  } else {
    bp <- cbind(Dataset = dat_name[i], Category = "BP", bp)
    print(paste0(nrow(bp)," significant BP terms for dataset ", dat_name[i]))
    sig_terms <- rbind(sig_terms, bp)
  }
  # MF terms
  MFGOdata <- new("topGOdata", ontology = "MF", allGenes = geneList, annot = annFUN.gene2GO, gene2GO = geneID2GO, nodeSize = 10)
  resultMFFisher <- runTest(MFGOdata, algorithm = "weight01", statistic = "fisher")
  MFterms <- GenTable(MFGOdata, classicFisher = resultMFFisher, orderBy = "classicFisher", ranksOf = "classicFisher", topNodes = 300)
  mf <- MFterms %>% filter(classicFisher < 0.01) %>% dplyr::select(any_of(c("GO.ID", "Term", "classicFisher")))
  if(nrow(mf) == 0){
    print(paste0(nrow(mf)," significant MF terms for dataset ", dat_name[i]))
  } else {
    mf <- cbind(Dataset = dat_name[i], Category = "MF", mf)
    print(paste0(nrow(mf)," significant MF terms for dataset ", dat_name[i]))
    sig_terms <- rbind(sig_terms, mf)  
  }
  # CC terms
  CCGOdata <- new("topGOdata", ontology = "CC", allGenes = geneList, annot = annFUN.gene2GO, gene2GO = geneID2GO, nodeSize = 10)
  resultCCFisher <- runTest(CCGOdata, algorithm = "weight01", statistic = "fisher")
  CCterms <- GenTable(CCGOdata, classicFisher = resultCCFisher, orderBy = "classicFisher", ranksOf = "classicFisher", topNodes = 300)
  cc <- CCterms %>% filter(classicFisher < 0.01) %>% dplyr::select(any_of(c("GO.ID", "Term", "classicFisher")))
  if(nrow(cc) == 0){
    print(paste0(nrow(cc)," significant CC terms for dataset ", dat_name[i]))
  } else {
    cc <- cbind(Dataset = dat_name[i], Category = "CC", cc)
    print(paste0(nrow(cc)," significant CC terms for dataset ", dat_name[i]))
    sig_terms <- rbind(sig_terms, cc)  
  }
}
```
Visualizar los resultados e identificar los términos de Ontología de Genes significativos.
